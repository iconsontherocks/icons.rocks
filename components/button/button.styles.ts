import styled from 'styled-components';
import { Card } from '../card/card';

export const ButtonWrapper = styled(Card)`
  padding: 20px 30px;
  color: white;
  text-transform: uppercase;
  font-size: 15px;
  font-weight: bold;
  letter-spacing: 0.2em;
  cursor: pointer;
  border: none;
  outline: none;
`;
