import styled from 'styled-components';
import { Card } from '../card/card';

export const InputWrapper = styled(Card)`
  border: none;
  outline: none;
  color: white;
  padding: 20px;
  font-size: 20px;
`;
